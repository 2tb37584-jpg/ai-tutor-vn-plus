# M09-04 — Expand Grade 8 Algebra Eval Corpus to 40 Cases

Status: DONE
Codex: YES

## Goal

Expand the canonical Grade 8 algebra evaluation corpus from exactly 20 to exactly 40 curated cases.

This batch must improve balance across the 10 mastery-bearing Grade 8 algebra skills rather than adding arbitrary volume.

Do not change eval-runner behavior.

## Current baseline

Current case count: 20

Current skill distribution:

- arithmetic.signed_number_operations: 1
- algebra.expression.distributive_property: 2
- algebra.expression.combine_like_terms: 2
- algebra.expression.simplify: 2
- algebra.equation.equivalent_transform: 1
- algebra.linear_equation: 5
- algebra.identity.basic: 2
- algebra.factorization: 2
- algebra.rational_expression.domain: 2
- algebra.rational_expression.simplify: 1

Current verifier fixtures:

- linear_equation: 3
- none: 17

No new linear-equation cases are required in this batch because that skill is already overrepresented.

## Target distribution after this task

Final case count: 40

Exact final skill counts:

- arithmetic.signed_number_operations: 4
- algebra.expression.distributive_property: 4
- algebra.expression.combine_like_terms: 4
- algebra.expression.simplify: 4
- algebra.equation.equivalent_transform: 4
- algebra.linear_equation: 5
- algebra.identity.basic: 4
- algebra.factorization: 4
- algebra.rational_expression.domain: 4
- algebra.rational_expression.simplify: 3

## Allowed implementation files

Modify only:

1. `evals/cases.jsonl`
2. `backend/tests/test_eval_runner.py`

## Forbidden scope

Do NOT modify:

- `backend/app/services/eval_runner.py`
- verifier implementation
- Tutor Engine
- taxonomy
- skill registry
- mastery
- misconception evidence
- frontend
- Docker
- dependencies
- migrations

Do not add verifier kinds.

## Preserve existing corpus

All existing 20 cases must remain unchanged.

Append exactly 20 UTF-8 JSONL records.

Each new case must:

- be one JSON object on one line;
- have a unique stable ID;
- use `grade: 8`;
- use `expected_first_state: "ask_attempt"`;
- use exactly one controlled mastery-bearing skill;
- have non-empty `expected_answer`;
- have useful non-empty `first_turn_must_not_contain`;
- not use `general.problem_solving`.

## Required 20 new cases

### Signed-number operations (+3)

1. `g8-signed-numbers-mixed-001`
   - Problem: `Tính -7 + 12 - 5.`
   - Skill: `arithmetic.signed_number_operations`
   - Expected answer: `0`
   - Forbidden answer forms should be contextual, e.g. `kết quả là 0`, `đáp án là 0`; do not forbid bare `0`.

2. `g8-signed-numbers-product-001`
   - Problem: `Tính (-4)(-3) + (-5).`
   - Skill: `arithmetic.signed_number_operations`
   - Expected answer: `7`
   - Use contextual forbidden answer forms; do not forbid bare `7`.

3. `g8-signed-numbers-division-001`
   - Problem: `Tính (-24)/6 - 2.`
   - Skill: `arithmetic.signed_number_operations`
   - Expected answer: `-6`
   - Use contextual forbidden answer forms; do not forbid bare `-6`.

### Distributive property (+2)

4. `g8-distributive-coefficient-001`
   - Problem: `Khai triển 4(2x - 3).`
   - Skill: `algebra.expression.distributive_property`
   - Expected answer: `8x - 12`

5. `g8-distributive-negative-002`
   - Problem: `Khai triển -2(3x + 5).`
   - Skill: `algebra.expression.distributive_property`
   - Expected answer: `-6x - 10`

### Combine like terms (+2)

6. `g8-combine-like-terms-002`
   - Problem: `Thu gọn 5x - 2 + 3x + 7.`
   - Skill: `algebra.expression.combine_like_terms`
   - Expected answer: `8x + 5`

7. `g8-combine-like-terms-negative-001`
   - Problem: `Thu gọn 4x - 9x + 6.`
   - Skill: `algebra.expression.combine_like_terms`
   - Expected answer: `-5x + 6`

### Expression simplify (+2)

8. `g8-expression-simplify-003`
   - Problem: `Rút gọn 2(3x - 1) - x + 4.`
   - Skill: `algebra.expression.simplify`
   - Expected answer: `5x + 2`

9. `g8-expression-simplify-negative-001`
   - Problem: `Rút gọn -3(x - 2) + 2(2x + 1).`
   - Skill: `algebra.expression.simplify`
   - Expected answer: `x + 8`

### Equivalent equation transformation (+3)

10. `g8-equivalent-transform-add-001`
    - Problem: `Từ 3x - 5 = 10, hãy viết phương trình tương đương sau khi cộng 5 vào cả hai vế.`
    - Skill: `algebra.equation.equivalent_transform`
    - Expected answer: `3x = 15`

11. `g8-equivalent-transform-divide-001`
    - Problem: `Từ 4x = 20, hãy viết phương trình tương đương sau khi chia cả hai vế cho 4.`
    - Skill: `algebra.equation.equivalent_transform`
    - Expected answer: `x = 5`

12. `g8-equivalent-transform-subtract-001`
    - Problem: `Từ x/3 + 2 = 7, hãy viết phương trình tương đương sau khi trừ 2 ở cả hai vế.`
    - Skill: `algebra.equation.equivalent_transform`
    - Expected answer: `x/3 = 5`

Do not add verifier fixtures to transformation-only cases.

### Basic identities (+2)

13. `g8-identity-square-difference-001`
    - Problem: `Khai triển (x - 5)^2 bằng hằng đẳng thức.`
    - Skill: `algebra.identity.basic`
    - Expected answer: `x^2 - 10x + 25`

14. `g8-identity-scaled-square-001`
    - Problem: `Khai triển (2x + 3)^2 bằng hằng đẳng thức.`
    - Skill: `algebra.identity.basic`
    - Expected answer: `4x^2 + 12x + 9`

### Factorization (+2)

15. `g8-factor-trinomial-002`
    - Problem: `Phân tích x^2 + 7x + 12 thành nhân tử.`
    - Skill: `algebra.factorization`
    - Expected answer: `(x+3)(x+4)`

16. `g8-factor-difference-squares-001`
    - Problem: `Phân tích x^2 - 9 thành nhân tử.`
    - Skill: `algebra.factorization`
    - Expected answer: `(x-3)(x+3)`

Do not add a factorization verifier fixture.

### Rational-expression domain (+2)

17. `g8-rational-domain-positive-001`
    - Problem: `Tìm điều kiện xác định của (2x + 1)/(x - 5).`
    - Skill: `algebra.rational_expression.domain`
    - Expected answer: `x != 5`

18. `g8-rational-domain-linear-denominator-001`
    - Problem: `Tìm điều kiện xác định của (x + 2)/(2x + 6).`
    - Skill: `algebra.rational_expression.domain`
    - Expected answer: `x != -3`

### Rational-expression simplify (+2)

19. `g8-rational-simplify-difference-squares-001`
    - Problem: `Rút gọn (x^2 - 9)/(x - 3) và nêu điều kiện xác định.`
    - Skill: `algebra.rational_expression.simplify`
    - Expected answer: `x + 3, x != 3`

20. `g8-rational-simplify-factor-001`
    - Problem: `Rút gọn (x^2 + 5x + 6)/(x + 2) và nêu điều kiện xác định.`
    - Skill: `algebra.rational_expression.simplify`
    - Expected answer: `x + 3, x != -2`

For both rational-simplify cases, forbidden phrases must protect both the simplified expression and the domain restriction.

## Leakage fixture quality

For symbolic answers, include spaced and compact variants when meaningful.

Examples:

- `8x - 12`
- `8x-12`

For equation answers, include forms like:

- `3x = 15`
- `3x=15`

For numeric-only answers, avoid bare numbers when they can cause false positives.

For rational-domain answers, include both ASCII and Unicode forms when appropriate:

- `x != 5`
- `x ≠ 5`

## Canonical regression test

Update the existing canonical-corpus regression in `backend/tests/test_eval_runner.py`.

It must assert:

1. exactly 40 cases load;
2. exact canonical ID set equals the original 20 IDs plus these 20 new IDs;
3. all cases have `grade == 8`;
4. all cases have `expected_first_state == "ask_attempt"`;
5. no expected skill is `general.problem_solving`;
6. represented skills equal exactly the 10 mastery-bearing skill codes;
7. per-skill counts equal the exact target distribution above;
8. `g8-linear-verifier-negative-001` remains present with `expected_valid is False`.

A local expected skill-count dictionary is acceptable.

## Existing verifier behavior

Preserve:

- only `linear_equation` verifier kind;
- existing 3 verifier fixtures;
- existing offline/live runner behavior;
- existing metric names.

None of the 20 new cases needs a verifier fixture.

## Acceptance criteria

Complete only if:

- all existing 20 cases remain unchanged;
- exactly 20 new cases are added;
- final corpus count is exactly 40;
- exact ID set is locked by test;
- exact final skill distribution matches target;
- all 10 mastery-bearing skills remain represented;
- no case uses `general.problem_solving`;
- all new cases are Grade 8 and expect `ask_attempt`;
- new rational-simplify cases require, rather than reveal, domain restrictions;
- no new verifier kind;
- no eval-runner change;
- only two allowed files change;
- targeted tests pass;
- bind-mounted canonical test passes;
- offline eval reports `"case_count": 40`;
- full backend passes.

## Required validation

Targeted:

```powershell
docker compose exec backend pytest -q tests/test_eval_runner.py
```

Canonical bind-mounted test:

```powershell
docker compose run --rm `
  -v "$($PWD.Path)\evals:/evals:ro" `
  -e AI_TUTOR_CANONICAL_EVAL_CASES=/evals/cases.jsonl `
  backend `
  pytest -q tests/test_eval_runner.py -k canonical
```

Offline eval:

```powershell
docker compose run --rm `
  -v "$($PWD.Path)\evals:/evals:ro" `
  backend `
  python -m app.services.eval_runner `
  --mode offline `
  --cases /evals/cases.jsonl
```

Expected:

```text
"case_count": 40
```

Full backend:

```powershell
docker compose exec backend pytest -q
```

Final scope:

```powershell
git diff --check
git status --short
git --no-pager diff --stat
```

Only `evals/cases.jsonl` and `backend/tests/test_eval_runner.py` may remain modified.

## Stop conditions

Stop and report rather than widening scope if implementation appears to require:

- `eval_runner.py` changes;
- verifier changes;
- taxonomy changes;
- Tutor Engine changes;
- frontend changes;
- Docker changes;
- dependencies;
- migrations;
- more than the two allowed files.
