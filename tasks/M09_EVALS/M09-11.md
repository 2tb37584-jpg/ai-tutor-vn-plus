# M09-11 — Correct Over-Broad Leakage Fixture for Repeated Factor

Status: READY
Codex: NO

## Goal

Correct the Grade 8 eval fixture for `g8-rational-simplify-repeated-factor-001` so the leakage metric does not fail merely because the tutor repeats the source expression `(x - 3)^2/(x - 3)`.

## Evidence from M09-08

The live first turn asked the student to identify the denominator and domain condition. It repeated `(x - 3)^2/(x - 3)` from the problem but did not reveal the simplified result.

The deterministic detector returned `false`.

The eval still failed because `first_turn_must_not_contain` includes `x - 3` / `x-3`, which are unavoidable source-expression fragments.

## Module / micro-task

Module: eval fixture quality.

## Allowed files

Modify only:

1. `evals/cases.jsonl`
2. one directly relevant eval-runner test file if needed for regression coverage

## Forbidden scope

Do not modify:

- runtime leakage detector;
- TutorAI prompts;
- eval scoring algorithm;
- skill registry;
- provider configuration;
- any unrelated corpus cases.

## Required change

For `g8-rational-simplify-repeated-factor-001`:

- remove forbidden phrases that are merely source-expression fragments such as bare `x - 3` / `x-3`;
- retain forbidden forms that actually reveal the final simplification/domain conclusion, such as the full answer and explicit domain condition;
- preserve the problem, expected skill, expected answer, grade, and expected state.

## Acceptance criteria

- a Socratic first turn may repeat the original expression without failing leakage;
- a turn that directly states the simplified result/domain condition still fails;
- fixture remains valid JSONL;
- corpus stays at 100 cases;
- eval fixture tests pass;
- no unrelated case changes.

## Tests

Run the existing eval fixture/runner tests and a targeted scorer regression for this case if an appropriate test file already exists.

Also verify:

```powershell
(Get-Content .\evals\cases.jsonl).Count
git diff --check
```

Expected corpus count remains `100`.
