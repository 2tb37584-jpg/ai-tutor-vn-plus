# M07-13 — Raise rational-expression domain skill to pilot-ready authored coverage

Status: DONE
Codex: YES — SMALL
Module: Skill Graph
Phase: Phase 3 pilot content readiness

## Direction gate

This task advances the Phase 3 pilot-readiness roadmap by closing the final measured authored-content gap. It is a bounded question-bank content task and does not broaden the tutoring core loop.

## Goal

Add exactly two trusted authored Grade 8 algebra domain-condition questions, raising `algebra.rational_expression.domain` from one authored item to the three-item pilot-readiness floor required by M09-23.

## Context

The current question bank contains 26 authored items and the M09-23 remaining authored-content gap is 2. The target skill currently has one authored item. M09-23 requires three distinct items per assessed skill for pre-test, measured learning exposure, and post-test roles.

The canonical `domain_condition` verifier family already exists. Its deterministic verifier compares the set of excluded exact rational values in `verification_reference` against candidate clauses of the form `x != value` or `x ≠ value`; multiple exclusions may be separated by comma or semicolon. No verifier or service capability change is expected.

The existing target record is:

```json
{
  "id": "g8alg.rational-expression-domain.001",
  "skill_code": "algebra.rational_expression.domain",
  "problem_text": "Tìm điều kiện xác định của phân thức (x+1)/(x-2).",
  "verification_reference": "2",
  "expected_answer": "x != 2",
  "verification_family": "domain_condition",
  "difficulty": 1
}
```

## Why now

The target skill is below the deterministic three-role content floor needed before the defined full-supported-domain pilot protocol can be considered ready.

## Exit relationship

This task is expected to reduce the documented M09-23 authored-content gap from 2 to 0. It does not claim full pilot readiness, measurable learner improvement, Phase 3 success, or permission to start Phase 4.

## Current authored coverage

| Skill | Current items |
| --- | ---: |
| `arithmetic.signed_number_operations` | 4 |
| `algebra.expression.distributive_property` | 3 |
| `algebra.expression.combine_like_terms` | 3 |
| `algebra.expression.simplify` | 3 |
| `algebra.identity.basic` | 3 |
| `algebra.linear_equation` | 3 |
| `algebra.equation.equivalent_transform` | 3 |
| `algebra.factorization` | 3 |
| `algebra.rational_expression.domain` | 1 |

Current bank total: 26 authored items. Current remaining M09-23 content gap: 2 items.

## Micro-task

Later implementation will add exactly these two records immediately after `g8alg.rational-expression-domain.001`, without changing their authored pairs:

```json
{
  "id": "g8alg.rational-expression-domain.002",
  "skill_code": "algebra.rational_expression.domain",
  "problem_text": "Tìm điều kiện xác định của phân thức (x-3)/(x+4).",
  "verification_reference": "-4",
  "expected_answer": "x != -4",
  "verification_family": "domain_condition",
  "difficulty": 1
}
```

```json
{
  "id": "g8alg.rational-expression-domain.003",
  "skill_code": "algebra.rational_expression.domain",
  "problem_text": "Tìm điều kiện xác định của phân thức (x+5)/((x-1)(x+2)).",
  "verification_reference": "1,-2",
  "expected_answer": "x != 1; x != -2",
  "verification_family": "domain_condition",
  "difficulty": 2
}
```

The mathematical intent is one excluded value, `x = -4`, for `.002`, and two excluded values, `x = 1` and `x = -2`, for `.003`. Later implementation must verify both pairs through the existing deterministic domain-condition verifier before or as part of adding them. No other skill or question is in scope.

## Expected post-task coverage

- `algebra.rational_expression.domain`: 3 authored items.
- Bank total: 28 authored items.
- Remaining M09-23 authored-content gap: 0 items.
- All nine assessed skills meet the three-item authored-content floor.
- Full pilot readiness: not claimed.
- Measurable learner improvement: not claimed.
- Phase 3 success: not claimed.
- Phase 4: not started.

## Architecture boundary

This is authored question-bank content and focused test coverage only. It must not change question-bank services, verifier behavior, taxonomy, recommendation, mastery, tutor, API, persistence, UI, or dependencies.

## Allowed files

Preparation files:

- `TASK_INDEX.md`
- `tasks/M07_SKILL_GRAPH/M07-13.md`

Later implementation files are limited to exactly:

- `backend/app/data/grade8_algebra_question_bank_v1.json`
- `backend/tests/test_question_bank.py`

## Forbidden scope

Do not modify services, verifier code, taxonomy or skill registry, recommender, mastery, tutor, API, models, schemas, Alembic, frontend, dependencies, Docker files, workflows, or other task files.

Do not add more than two questions, touch another skill, modify existing authored records, change verifier behavior, add DB/API/UI/schema/migration work, add dependencies, claim pilot readiness, claim Phase 3 success, start Phase 4, commit, push, or open a PR during preparation.

## Acceptance checklist

- [x] Exactly two authored questions are added.
- [x] `g8alg.rational-expression-domain.002` is added.
- [x] `.002` uses skill `algebra.rational_expression.domain`.
- [x] `.002` uses verifier family `domain_condition`.
- [x] `.002` uses difficulty 1.
- [x] `.002` self-verifies as CORRECT.
- [x] `g8alg.rational-expression-domain.003` is added.
- [x] `.003` uses skill `algebra.rational_expression.domain`.
- [x] `.003` uses verifier family `domain_condition`.
- [x] `.003` uses difficulty 2.
- [x] `.003` self-verifies as CORRECT.
- [x] Target skill has at least 3 authored items.
- [x] Both new IDs are unique.
- [x] New problem text does not reveal expected answers.
- [x] Existing authored records remain unchanged.
- [x] Production declaration order remains deterministic.
- [x] Bank total becomes 28.
- [x] No fixed total-bank-count loader invariant is introduced.
- [x] No fixed verifier-family-count invariant is introduced.
- [x] M09-23 remaining content gap is reduced from 2 to 0.
- [x] All nine assessed skills meet the three-item authored-content floor.
- [x] Full pilot readiness is not claimed.
- [x] Phase 3 success is not claimed.
- [x] Measurable learner improvement is not claimed.
- [x] No question-bank service, verifier, or taxonomy/skill-registry changes.
- [x] No recommendation/mastery/tutor changes.
- [x] No DB/API/schema/migration/UI changes.
- [x] No dependency is added.
- [x] Targeted question-bank tests pass.
- [x] Relevant question-recommender tests pass.
- [x] Full backend regression passes.
- [x] Final implementation diff is limited to the two allowed implementation files.
- [x] No real learner data is introduced.
- [x] Phase 4 is not started.

## Tests

Later implementation should run:

```powershell
.\backend\.venv\Scripts\python.exe -m pytest -q backend\tests\test_question_bank.py
.\backend\.venv\Scripts\python.exe -m pytest -q backend\tests\test_question_recommender.py
.\backend\.venv\Scripts\python.exe -m pytest -q backend\tests
```

Do not run live/provider tests.

## Validation plan

Preparation validation must confirm the lifecycle is active and consistent, exactly 34 acceptance items remain unchecked, the two task-control files are the only preparation changes, and `git diff --check` passes. Later implementation validation must confirm the exact authored records, deterministic domain-condition self-verification, targeted question-bank tests, recommender regression, and full backend regression.

## Follow-up boundary

After this task, authored-content readiness is complete for the nine-skill blueprint, but pilot execution and measurement remain separate work. Do not create or implement those follow-ups here.

## Validation evidence

- ChatGPT reviewed the complete implementation diff.
- `REVIEW_RESULT: IMPLEMENTATION_APPROVED`.
- Exactly two authored questions were added: `g8alg.rational-expression-domain.002` and `g8alg.rational-expression-domain.003`.
- `algebra.rational_expression.domain` now has 3 authored items.
- Both use canonical `domain_condition`; `.002` uses difficulty 1 and `.003` uses difficulty 2.
- Both verification pairs self-verify as CORRECT.
- Pre-change deterministic domain-condition verifier gate: PASS.
- Question bank total is now 28.
- Remaining M09-23 authored-content gap is 0.
- All nine assessed skills meet the three-item authored-content floor.
- Question-bank tests: `56 passed`.
- Recommender regression: `30 passed`.
- Full backend regression: `652 passed, 13 warnings`.
- Warnings are existing `datetime.utcnow()` deprecation warnings outside M07-13 scope.
- No `question_bank.py` change.
- No verifier/taxonomy/skill-registry changes.
- No recommendation/mastery/tutor changes.
- No DB/API/schema/migration/UI/dependency changes.
- No real learner data added.
- Full pilot readiness is not claimed.
- Measurable learner improvement is not claimed.
- Phase 3 success is not claimed.
- Phase 3 still requires real pilot evidence of measurable post-test improvement.
- Phase 4 not started.
- `git diff --check`: PASS.

## Stop conditions

Stop and report without expanding scope if another task is active, the current target count is not 1, the bank total is not 26, the canonical verifier family differs from `domain_condition`, either proposed pair is incompatible with the deterministic domain-condition verifier, lifecycle consistency is false, more than the two allowed preparation files need modification, service/verifier changes appear necessary, or real learner data would be required.

## Delivery workflow

Implementation has been reviewed and validation has passed. The lifecycle may now be closed. Commit, push, and PR actions are allowed only after ChatGPT reviews this closure diff. M07-13 is DONE once lifecycle documentation is consistent.
