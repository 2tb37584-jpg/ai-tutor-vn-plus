# M00-08 — Fix Alembic Revision-ID Startup Blocker

Status: ACTIVE
Codex: YES
Module: Baseline / Alembic

## Goal

Restore normal backend startup by making Alembic revision `0005` compatible with the default `alembic_version.version_num VARCHAR(32)` column.

## Context

Backend startup runs:

```text

alembic upgrade head

→ uvicorn

```

The current Alembic head is:

```text

0005_tutor_session_verification_family

```

Revision chain:

```text

0003_internal_expected_answer

→ 0004_tutor_message_reply_latency

→ 0005_tutor_session_verification_family

```

`0005_tutor_session_verification_family` is longer than 32 characters.

During backend startup, PostgreSQL rejects Alembic's version-table update:

```text

sqlalchemy.exc.DataError:

(psycopg.errors.StringDataRightTruncation)

value too long for type character varying(32)

UPDATE alembic_version

SET version_num='0005_tutor_session_verification_family'

WHERE alembic_version.version_num='0004_tutor_message_reply_latency'

```

The migration itself is the current single Alembic head and has no downstream migration depending on its revision identifier.

The backend therefore exits before Uvicorn starts.

This blocker was discovered while closing M08-05. M08-05 itself is DONE and must not be reopened or modified.

## Micro-task

Shorten only the Alembic revision identifier for migration `0005` so that it fits within 32 characters.

Preferred identifier:

```text

0005_tutor_verifier_family

```

Keep:

```text

down_revision = "0004_tutor_message_reply_latency"

```

Do not change the migration's schema operations.

## Allowed production file

```text

backend/alembic/versions/0005_tutor_session_verification_family.py

```

## Task-control files

```text

TASK_INDEX.md

tasks/M00_BASELINE/M00-08.md

```

Task-control changes are separate from the production implementation scope.

## Forbidden scope

Do not modify:

```text

backend models

application services

API routes or schemas

Docker startup commands

alembic/env.py

0004 or earlier migrations

database application schema

alembic_version table schema

dependencies

frontend

M08 recommender code/tests

```

Do not:

```text

add migration 0006

increase alembic_version.version_num width

rewrite migration history

change upgrade/downgrade operations in 0005

add compatibility abstractions

perform unrelated refactors

```

## Required implementation

In:

```text

backend/alembic/versions/0005_tutor_session_verification_family.py

```

change the revision metadata from:

```python

revision: str = "0005_tutor_session_verification_family"

```

to:

```python

revision: str = "0005_tutor_verifier_family"

```

Update the revision identifier in the migration file header/docstring if it repeats the old ID.

Do not change:

```python

down_revision

upgrade()

downgrade()

```

The final revision identifier must be at most 32 characters.

## Acceptance checklist

- [ ] Revision `0005` identifier is at most 32 characters.

- [ ] Revision identifier is `0005_tutor_verifier_family`.

- [ ] `down_revision` remains `0004_tutor_message_reply_latency`.

- [ ] Migration `upgrade()` body is unchanged.

- [ ] Migration `downgrade()` body is unchanged.

- [ ] No new migration is added.

- [ ] No application schema/model change is added.

- [ ] No `alembic_version` table alteration is added.

- [ ] Alembic reports exactly one head.

- [ ] The single head is the shortened `0005` revision.

- [ ] `alembic upgrade head` succeeds against the existing development database.

- [ ] `alembic current` reports the shortened `0005` revision.

- [ ] Normal backend startup reaches and keeps Uvicorn running.

- [ ] `docker compose ps` shows backend running after migration startup completes.

- [ ] Full backend regression passes.

- [ ] Final implementation diff is limited to the one allowed production file.

- [ ] No dependency is added.

## Verification

Before implementation capture:

```powershell

docker compose run --rm --no-deps backend alembic heads

```

Expected current failing head:

```text

0005_tutor_session_verification_family (head)

```

After implementation run:

```powershell

docker compose run --rm --no-deps backend alembic heads

```

Expected:

```text

0005_tutor_verifier_family (head)

```

Then run normal startup:

```powershell

docker compose up -d

docker compose ps

```

Verify the backend remains running rather than exiting during Alembic startup.

Then:

```powershell

docker compose exec backend alembic current

```

Expected current revision:

```text

0005_tutor_verifier_family

```

Then run:

```powershell

docker compose exec backend pytest -q

```

Expected regression baseline is at least:

```text

504 passed

1 skipped

7 existing warnings

```

Warnings from legacy `datetime.utcnow()` behavior in `backend/app/services/mastery.py` are outside this task.

## Review requirements

Before commit verify:

```powershell

git diff --check

git diff -- backend/alembic/versions/0005_tutor_session_verification_family.py

git status --short

```

The migration diff must contain only revision metadata/header changes.

The SQL/schema operations inside `upgrade()` and `downgrade()` must be unchanged.

## Codex

Yes — small.

Codex must be limited to the single production file:

```text

backend/alembic/versions/0005_tutor_session_verification_family.py

```

Codex must not edit task-control files and must not commit.

## Stop conditions

Stop and report instead of broadening scope if:

```text

a downstream migration depends on the old 0005 revision

the database is already stamped with the oversized 0005 revision

changing the identifier alone does not allow alembic upgrade head

another production file must change

alembic_version schema must change

a new migration appears necessary

migration upgrade/downgrade operations need modification

```
